---
trigger: always_on
---

- Follow the current structure of the code
- extension.ts is the entry point of the extension
- Isolate functionallity in different files
- Use the existing classes when possible (create new classes otherwise)